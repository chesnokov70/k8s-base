apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: local-storage
provisioner: kubernetes.io/no-provisioner # epmty
volumeBindingMode: WaitForFirstConsumer  # it will create when pod created
